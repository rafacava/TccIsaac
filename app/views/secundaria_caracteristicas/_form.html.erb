<%= form_with(model: secundaria_caracteristica, local: true) do |form| %>
  <% if secundaria_caracteristica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secundaria_caracteristica.errors.count, "error") %> prohibited this secundaria_caracteristica from being saved:</h2>

      <ul>
      <% secundaria_caracteristica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
