<%= form_with(model: principal_fator, local: true) do |form| %>
  <% if principal_fator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(principal_fator.errors.count, "error") %> prohibited this principal_fator from being saved:</h2>

      <ul>
      <% principal_fator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
