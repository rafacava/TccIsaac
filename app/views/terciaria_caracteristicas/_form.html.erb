<%= form_with(model: terciaria_caracteristica, local: true) do |form| %>
  <% if terciaria_caracteristica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(terciaria_caracteristica.errors.count, "error") %> prohibited this terciaria_caracteristica from being saved:</h2>

      <ul>
      <% terciaria_caracteristica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
